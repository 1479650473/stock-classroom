# 2026-07-08 日记

## stock-classroom 全面复盘与修复（00:24-01:05）
先生觉得项目底层不满意但说不清问题，我逐文件分析后找到 12 个问题，全部修复。

### P1-P4 共 9 项修复
1. P1-1 表归属清理
2. P1-2 退市股修复
3. P2-1 定时缓存 + 北向资金接口修复
4. P2-2 K线前向填充
5. P2-3 评分回测验证
6. P3-1 数据源统一
7. P3-2 死代码清理
8. P4-1 缓存路由
9. P4-2 前端模块化

55/55 自动化测试全通过。

## 第二轮：数据库教学 + 环境规范化（02:17-02:48）

### 先生从零学数据库
- 讲解了"表就是表格，列对不上就拿不到数"
- 用北向资金的例子：AKShare 返回列名是中文（日期/当日成交净买额），代码读的却是英文（date/value/amount），导致 2703 行全变成空值
- 装了 DB Browser for SQLite 可视化工具，先生可以像 Excel 一样看数据库

### 修复了 3 个数据库 Bug
1. **north_flow 列名映射缺失** → 加了 col_map 映射（日期→date, 当日成交净买额→net_flow, 历史累计净买额→cum_flow）
2. **sector_data INSERT 列错位** → 第4列 price 被塞了 change_pct，已修正为 price → price
3. **market.js Uncaught ReferenceError** → app.js IIFE 后 API 变量出不去，加了 window.API 暴露

### 依赖规范化
- 创建了 `pyproject.toml`（项目身份证）
- 创建了 `requirements.txt`（锁定版本）
- 核心依赖：flask 3.0.0, flask-cors 4.0.0, akshare 1.18.64, pandas 3.0.1, numpy 2.4.2, requests 2.32.5
- 开发依赖：backtesting 0.6.5, matplotlib 3.10.8, scipy 1.17.1
- 验证通过了 py_compile（语法无错）、API 测试（market/kline/search/cache 全部返回正常）

### 清理
- 删了 __pycache__、temp/test_venv、两个备份 db 文件
- 项目目录清爽了

### 待办
- north_flow 缓存数据需要重新刷新（代码已修，只差执行一次缓存刷新）
- 先生想做成安装包分发的桌面软件，讨论了两个方向（Electron 套壳 vs 重构）
